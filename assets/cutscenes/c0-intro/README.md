# C0_INTRO assets

- Runtime inserts: `inserts/*.png` (336×216)
- Final knock background: `backgrounds/office-closed-door.png` (640×384)
- Generated sources and prompt record: `source/`
- Shared player portraits and the office composition remain locked to the approved `c0b` asset set.
  The final knock beat uses a generated closed-door variant of the same room so the detailed woodwork,
  hardware, lighting and perspective match the other finished cutscene images.

Rebuild with:

```bash
python3 scripts/prepare-c0-intro-assets.py
```
