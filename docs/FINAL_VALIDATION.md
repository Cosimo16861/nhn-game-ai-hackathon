# 최종 개발 검수 결과

검수일: 2026-07-30  
브랜치: `codex/pixel-reconstruction-game`  
검수 기준 커밋: `77fb8d3`

## 구현 상태

- 대형 사건 선택 → 세부 사건 선택 → 복원 → 결과 → 다음 세부 사건 흐름
- 2×2 세부 사건 네 개의 순차 해제와 전체 그림 조립
- 목격담 A·B·C와 불규칙 가림 구역 연결
- 128×128 색칠판과 256×256 분석·채점
- 브러시, 채우기, 실행 취소, 확대, 힌트
- 복원 점수와 CLIP 의미 점수를 결합한 단일 결과 점수
- 난이도별 규칙, 통과 점수, 별점, 브라우저 진행 저장
- 전체 그림 완성 후 최종 추리 문제
- 키보드, 모바일 터치 영역, 포커스 이동, 모션 감소 대응

## 콘텐츠 준비 상태

| 대형 사건 | 상태 | 세부 사건 | 비고 |
| --- | --- | ---: | --- |
| 박물관 절도 사건 | 준비 완료 | 4/4 | 최종 추리 포함 |
| 심야 편의점 절도 사건 | 임시 콘텐츠 | 1/4 | 화면에서 선택 불가 |

편의점 사건은 연속된 전체 이미지, 세부 사건 세 개, 단서와 최종 추리를 추가한 뒤 `masterImageStatus`를 `ready`로 바꿔야 합니다. 자세한 절차는 `docs/CONTENT_EXPANSION_GUIDE.md`에 있습니다.

## 자동 검수 결과

- `node --test tests/*.test.cjs`: 23개 통과, 실패 0
- `node --check`: `scripts`의 모든 JavaScript 및 CommonJS 파일 통과
- `git diff --check`: 공백 오류 없음
- 로컬 HTTP 응답:
  - `/index.html`: 200, `text/html`
  - `/scripts/app.js`: 200, `text/javascript`
  - `/assets/cases/museum-robbery-master-dark-color.png`: 200, `image/png`

## 실행 전제와 남은 작업

- `file://` 직접 열기보다 `node scripts/dev-server.cjs`로 실행해야 합니다.
- CLIP 모델은 최초 실행 때 외부 모델 파일을 내려받으므로 인터넷 연결이 필요합니다.
- 남은 핵심 작업은 코드 기능 구현이 아니라 편의점 사건 콘텐츠 완성, 실제 플레이 테스트, 제출 자료 준비입니다.
- 현재 편집 내용은 아직 커밋하거나 원격 저장소에 푸시하지 않았습니다.
