# 최종 통합 QA

검증일: 2026-08-10  
검증 기준: `d2f12b1` (`game` 브랜치)

## 결과

통합 게임의 제품 진입점은 `index.html` 하나이며, 자동 검사와 핵심 브라우저
스모크 테스트를 통과했다. 이번 단계에서는 제품 코드를 수정하지 않았다.

## 자동 검사

- `node --test tests/*.test.cjs`: 20/20 통과
- `node --test tests/full-game-main-route.e2e.cjs`: 1/1 통과
- 메인 루트 시뮬레이션: 시작 화면, 오프닝, Q0~Q5A, Q6, 엔딩,
  이어하기, 새 게임 초기화까지 통과
- 퀘스트 등록: 14개
- 완료 컷신 번들: 16개
- 퀘스트 이미지: 28개
- 컷신 자산: 166개
- `src`, `scripts`, `dev` JavaScript 문법 검사 통과
- 제품 런타임의 외부 네트워크 의존성 없음

`scripts/clip-analyzer.js`의 CDN 참조는 Q1A 개발 검토 페이지에서만 사용되며
`index.html` 제품 런타임에는 로드되지 않는다.

## 브라우저 스모크 테스트

로컬 정적 서버에서 다음 경로를 실제 UI로 확인했다.

1. 시작 화면에서 프롤로그 다시 보기 → 오프닝 컷신 진입
2. 이어하기 → 저장된 증거판 복구
3. 증거판에서 Q1A 선택 → 1254×1254 복원 작업대 진입
4. 팔레트, 16/36/72px 붓, 채우기, 지우개, 실행 취소/다시 실행,
   확대/축소, 제출 및 증거판 복귀 컨트롤 노출 확인
5. 브라우저 콘솔 경고·오류 0건

## 검증 범위의 구분

- 전체 메인 루트와 새로고침 복구는 의존성 없는 E2E 시뮬레이션으로 검증했다.
- 브라우저에서는 비용을 제한하기 위해 시작·컷신·이어하기·증거판·고해상도
  작업대의 핵심 수직 슬라이스만 수동 확인했다.
- 14개 그림을 사람이 직접 칠해 엔딩까지 가는 장시간 실플레이는 수행하지 않았다.
- 레거시 삭제는 이번 QA 범위가 아니다. 삭제 전에는
  `LEGACY_DELETION_MANIFEST.md`의 별도 승인이 필요하다.

## 판정

현재 통합본은 기능 계약과 메인 진행 경로 기준으로 릴리스 후보 상태다.
배포 전 남은 권장 작업은 실제 사용자 1회의 전 구간 플레이테스트와 난이도·통과
기준 체감 조정이다.
